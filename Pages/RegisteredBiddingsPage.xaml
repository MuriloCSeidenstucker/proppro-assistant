<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PropproAssistant.Pages.RegisteredBiddingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PropproAssistant.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock x:Name="TxtBl_Bidding" Grid.Row="0" Grid.Column="1"/>
        <TextBlock Grid.Row="1" Grid.Column="0">Itens:</TextBlock>
        <TextBlock x:Name="TxtBl_ItemNumber" Grid.Row="2" Grid.Column="0"/>
        <TextBlock x:Name="TxtBl_ItemDescription" Grid.Row="2" Grid.Column="1"/>
        <TextBlock x:Name="TxtBl_ItemCostValue" Grid.Row="2" Grid.Column="2"/>
    </Grid>
</Page>
